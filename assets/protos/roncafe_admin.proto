syntax = "proto3";

option csharp_namespace = "RonCafeApp.Grpc";

package admin;

service AdminService {
  rpc SelectWindow (WindowRequest) returns (CommandResponse);
}

message WindowRequest {
  string window_type = 1; // e.g., "MainWindow" or "MainWindow2"
}

// Response message: indicates if the operation was successful
message CommandResponse {
  bool success = 1;
  string message = 2;
}